import React from "react";
import {Login} from "../components";

const LoginPage = () => {
    return (
        <div className="principal">
        <Login/>
        </div>
    );
};

export default LoginPage;